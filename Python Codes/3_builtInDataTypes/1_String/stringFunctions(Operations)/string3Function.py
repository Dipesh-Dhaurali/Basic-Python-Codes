# find the length of an integer

a=122323

# print(len(a)) it throws an error because len function is only use in string

dig=str(a)   # it converts the integer to string
print(len(dig)) # now we can find the length of dig which is string