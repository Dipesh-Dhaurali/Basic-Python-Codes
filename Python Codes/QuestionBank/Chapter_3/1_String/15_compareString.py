# Write a Python program to check if two strings are anagrams
# (contain the same characters in a different order).

str1="Dipesh"
str2="Kritika"

if len(str1)==len(str2):
    print("The both string are anagrams")

else:
    print("The strings are not anagrams")