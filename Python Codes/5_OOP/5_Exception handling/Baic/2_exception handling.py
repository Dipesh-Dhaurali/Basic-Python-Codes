"""
It is a mechanism through which we can handle the
runtime error.


To solve this error in python, we use
--------------------------
1) try
2) except
3) else


#types of error to handle exception handling
-------------------------------------------
1) ArithmeticError  (ZeroDivisionError)
2) OverflowError (Numerical result too large to represent.)
3) FloatingPointError ( Floating point operation failure (rarely raised manually).)
4) IndexError ( Index out of range (e.g., accessing a list element that doesn’t exist).)
5) TypeError
6) ValueError
7) Custom error
"""